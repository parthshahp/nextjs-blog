// Your Metadata type remains the same
export type Metadata = {
  title: string;
  slug: string;
  date: string;
  updated?: string;
  published: boolean;
  description: string;
  tags?: string[];
};
